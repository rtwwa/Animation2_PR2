public class Constants {
    // add
    // -Dsun.java2d.uiScale=1.0
    // in VM Options if OS Scale not 1
    public static final int MAX_WIDTH = 600;
    public static final int MAX_HEIGHT = 500;
    public static final double SCALE = 1.25;
}
